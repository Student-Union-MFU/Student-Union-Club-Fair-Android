<?xml version="1.0" encoding="utf-8"?>
<!--
  API 24 and 25, for launchers that ask for `android:roundIcon`. Circular, not
  the rounded square `ic_launcher` uses — see `drawable/ic_launcher_legacy_round.xml`.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@drawable/ic_launcher_legacy_round" />
</layer-list>
